<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.hume.gamemap">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <application android:allowBackup="true" android:label="@string/app_name"
        android:hardwareAccelerated="false"
        android:icon="@drawable/ic_launcher" android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
                <meta-data
                    android:name="android.support.UI_OPTIONS"
                    android:value="splitActionBarWhenNarrow" />
            </intent-filter>
        </activity>
        <activity android:name=".MainDotaActivity"
            android:label="dota主界面"></activity>
        <activity android:name=".HeroDetailActivity"
            android:label="英雄详细界面"
            android:theme="@style/Theme.Slidr.Slidable"></activity>
        <activity android:name=".ItemsDetailActivity"
            android:label="物品详细界面"
            android:theme="@style/Theme.Slidr.Slidable"></activity>
    </application>

</manifest>
